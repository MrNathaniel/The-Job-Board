const Jobs = ({ className, title, contractType, country, city }) => {
  return (
    <>
      <div>
        {props.className}
        {props.title}
        {props.contractType}
        {props.country}
        {props.city}
      </div>
    </>
  );
};

export default Jobs;
